cli 客户端 使用方式：

# 启动 REST API 服务器
penshot serve-rest --host 0.0.0.0 --port 8000

# 启动 MCP 服务器
penshot serve

# 查看队列状态
penshot queue-status

# 其他命令
penshot breakdown "剧本内容" -o result.json
penshot status <task_id>
penshot result <task_id>
penshot cancel <task_id>
penshot batch -f scripts.txt --sync